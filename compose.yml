services:
  wedding-invitation:
    build:
      context: .
      dockerfile: Containerfile
    container_name: wedding-invitation
    restart: unless-stopped
    ports:
      - "8070:8070"
    networks:
      - public

networks:
  public:
    external: true
